const Enum = require('../../common/enum')

module.exports = {
  ADDRESS_SYMBOL: new Enum('ADDRESS_SYMBOL', Symbol('ADDRESS_SYMBOL')),
  IMMEDIATE_SYMBOL: new Enum('IMMEDIATE_SYMBOL', Symbol('IMMEDIATE_SYMBOL')),
  LABEL_SYMBOL: new Enum('LABEL_SYMBOL', Symbol('LABEL_SYMBOL')),
}
